<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muy.constrainlayoutlearning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CommonActivity" />
        <activity android:name=".CenterActivity" />
        <activity android:name=".RatioActivity" />
        <activity android:name=".ArcActivity" />
        <activity android:name=".LimitedActivity" />
        <activity android:name=".ChainStyleActivity" />
        <activity android:name=".GuideLineActivity" />
        <activity android:name=".GroupActivity" />
        <activity android:name=".CircularRevealActivity" />
        <activity android:name=".PlaceHolderActivity" />
        <activity android:name=".ConstrainSetActivity" />
        <activity android:name=".FlowActivity" />
    </application>

</manifest>